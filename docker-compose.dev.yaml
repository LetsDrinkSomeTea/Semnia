services:
  backend:
    build:
      context: .
      target: backend        # skips the frontend Node stage
    restart: unless-stopped
    volumes:
      - ./backend/app:/backend/app   # hot reload: mounts source over baked image
      - data-dev:/data
      - semnia-model-cache:/model-cache
    environment:
      - MEILISEARCH_URL=http://meilisearch:7700
      - MEILISEARCH_KEY=semnia_local_dev_key
      # - EMBEDDING_MODEL=intfloat/multilingual-e5-large
      # - LLM_URL=http://ollama:11434
      # - LLM_MODEL=llama3.2:1b
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8000/api/status')"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 120s

  frontend:
    image: node:20-slim
    working_dir: /app
    volumes:
      - ./frontend:/app
      - frontend_nm:/app/node_modules  # named volume keeps node_modules across restarts
    ports:
      - "5173:5173"
    environment:
      - VITE_API_TARGET=http://backend:8000
    command: sh -c "npm install && npm run dev -- --host 0.0.0.0 --port 5173"
    depends_on:
      backend:
        condition: service_healthy

  ollama:
    image: ollama/ollama
    restart: unless-stopped
    volumes:
      - ollama-dev:/root/.ollama
    entrypoint: ["/bin/sh", "-c", "ollama serve & sleep 8 && ollama pull llama3.2:1b; wait"]
    profiles:
      - llm

  meilisearch:
    image: getmeili/meilisearch:v1.10
    restart: unless-stopped
    environment:
      - MEILI_NO_ANALYTICS=true
      - MEILI_MASTER_KEY=semnia_local_dev_key
    ports:
      - "7700:7700"  # Expose in dev for easy debugging
    volumes:
      - meili_dev_data:/meili_data

volumes:
  data-dev:
  semnia-model-cache:
  frontend_nm:
  ollama-dev:
  meili_dev_data:
